# NeverMiss — Product Backlog
_Last updated: 2026-05-01 | Maintained by Agent 4 (Changelog Manager / Documentation Owner / Backlog Curator)_

## Recently Completed (Sprint 2 — Iterations 16–18)

| Task ID | Task | Priority | Status | Sprint | Type | Files Changed | Description |
|---------|------|----------|--------|--------|------|---------------|-------------|
| BL-039 | BUG: Calendar Birthdays not displayed | 🔴 High | ✅ Done | Sprint 2 | Bug | CalendarScreen.tsx, hebrewDateUtils.ts, i18n/index.ts, types/index.ts | Contact birthdays (and Hebrew birthdays) now appear as pink dots on calendar cells; birthday section panel shows contact names for selected day or full month |
| BL-040 | FEATURE: Hebrew Birthday support | 🟡 Medium | ✅ Done | Sprint 2 | Feature | types/index.ts, hebrewDateUtils.ts, ContactFormScreen.tsx, i18n/index.ts | Optional `hebrewBirthday` field on Contact ("DD-MM" format); Day+Month dropdowns in premium ContactForm; `hebrewBirthdayToGregorianInCalendarYear()` converts to Gregorian for any calendar year; Calendar uses Hebrew date when available |
| BL-014 | RELIGION_LABELS i18n | 🔴 High | ✅ Done | Sprint 2 | Improvement | CalendarScreen.tsx, HolidayCard.tsx | Already implemented — t(`religion_${...}`) in use; all keys present in both EN+HE |
| BL-015 | GreetingEditorScreen tier desc i18n | 🟡 Medium | ✅ Done | Sprint 2 | Improvement | GreetingEditorScreen.tsx, i18n/index.ts | Tier descs + signature placeholder via t(); 4 new i18n keys; aria-labels on Regenerate/Copy |
| BL-016 | ContactCard action label translation | 🟡 Medium | ✅ Done | Sprint 2 | Improvement | ContactCard.tsx | ACTION_KEY map + useT(); action label shown in Hebrew when language=he |
| BL-017 | Missing aria-labels on icon-only buttons | 🟡 Medium | ✅ Done | Sprint 2 | Accessibility | ContactDetailScreen.tsx, GreetingEditorScreen.tsx | aria-label via t() on Edit, Regenerate, Copy buttons |
| BL-018 | aria-expanded on toggle buttons | 🟡 Medium | ✅ Done | Sprint 2 | Accessibility | GreetingEditorScreen.tsx | aria-expanded on Advanced tone + Signature toggles; WCAG 4.1.2 |
| BL-041 | Celebration feedback on Mark as Contacted | 🟡 Medium | ✅ Done | Sprint 2 | Improvement | ContactDetailScreen.tsx, index.css | 6-emoji particle burst + Web Audio ping (880→1200Hz) + haptic (navigator.vibrate 40ms); respects prefers-reduced-motion |
| BL-019 | React.lazy for remaining screens | 🟡 Medium | ✅ Done | Sprint 2 | Performance | App.tsx | 13 screens converted to lazy chunks; Suspense fallback inside WithNav; main bundle 435→285 kB (37 kB gzip saved); premium route inner Suspense simplified |
| BL-020 | scoringSystem.ts memoization | 🟢 Low | ✅ Done | Sprint 2 | Performance | src/core/scoringSystem.ts | Module-level Map cache keyed by `id|updatedAt`; cache hit prevents all recalculation for unchanged contacts; stale results impossible since updatedAt is always set on mutation |
| BL-021 | Pull-to-refresh on Dashboard | 🟢 Low | ✅ Done | Sprint 2 | UX | DashboardScreen.tsx | Already implemented — touchStart/Move/End handlers; PULL_THRESHOLD=64px; RefreshCw visual indicator; i18n keys dashboard_pull_refresh / dashboard_release_refresh |
| BL-022 | Dashboard refresh button (no-op) | 🟢 Low | ✅ Done | Sprint 2 | Technical Debt | DashboardScreen.tsx | No standalone refresh button exists; RefreshCw used only in pull indicator; refreshDashboard() stub wired to pull gesture only — no dead button |

> Source of truth: FEATURES.md (features), BUG_REPORT.md (bugs), AGENTS.md (agents).
> BACKLOG.md and BACKLOG.csv are planning views derived from those sources. Do NOT edit directly — update the source first.

---

## Sprint 2 — ✅ COMPLETE

All Sprint 2 tasks are done. See "Recently Completed" section above.

---

## Sprint 6 — ✅ COMPLETE

| Task ID | Task | Priority | Status | Sprint | Type | Files Changed | Description |
|---------|------|----------|--------|--------|------|---------------|-------------|
| BL-053 | FINDING 86: Extract SUGGESTED_BASES to groupSuggestions.ts | 🟢 Low | ✅ Done | Sprint 6 | Tech Debt | src/data/groupSuggestions.ts, GroupsScreen.tsx | New shared data file; GroupsScreen imports SUGGESTED_BASES from it |
| BL-054 | FINDING 75/87: applyPurpose prunes old-purpose suggestions | 🟡 Medium | ✅ Done | Sprint 6 | Bug | GroupsScreen.tsx | Purpose change now removes previous purpose's suggestions before adding new ones |
| BL-055 | FINDING 31: Remove redundant "1:00" from recording UI | 🟢 Low | ✅ Done | Sprint 6 | UX | MediaAttachmentPicker.tsx | media_record_limit key already says "Max 60 seconds"; removed duplicate hardcoded suffix |
| BL-056 | FINDING 49: "Send to group" navigates to greeting editor | 🟡 Medium | ✅ Done | Sprint 6 | UX | DashboardScreen.tsx | CTA now opens /greeting?contactId=&holidayId= for first group contact instead of generic /contacts |
| BL-057 | FINDING 76/81: findDuplicate covers edits + self-exclusion | 🟡 Medium | ✅ Done | Sprint 6 | Bug | ContactFormScreen.tsx | Duplicate check runs on save for both new and edited contacts; self-excluded by contact.id |
| BL-058 | FINDING 84/85: Group assign robustness in ImportContactsScreen | 🟡 Medium | ✅ Done | Sprint 6 | Bug | ImportContactsScreen.tsx, i18n/index.ts | Card hidden when 0 contacts imported; try/catch + Hebrew error toast on assign failure |

---

## Sprint 3/4 — In Progress

| Task ID | Task | Priority | Status | Sprint | Type | Files Changed | Description |
|---------|------|----------|--------|--------|------|---------------|-------------|
| BL-043 | BUG: Multi-day holidays show only first day | 🔴 High | ✅ Done | Sprint 3/4 | Bug | CalendarScreen.tsx | getHolidaysForDay now checks [date, endDate] range; monthly list shows holidays overlapping the visible month |
| BL-044 | BUG: Sukkot 2026 missing | 🔴 High | ✅ Done | Sprint 3/4 | Bug | holidays.ts | Added sukkot-2026 (Sep 25–Oct 2, 2026 / 15–22 Tishrei 5787) |
| BL-045 | IMPROVEMENT: Celebration feedback on Mark as Contacted | 🟡 Medium | ✅ Done | Sprint 3/4 | Improvement | ContactDetailScreen.tsx, index.css | Sound: C5/E5/G5 major triad arpeggio replaces harsh oscillator. Animation: 10 particles (up from 6), text-xl, scale-pop keyframe, wider spread (±85px h, ±110px v), 1.4s duration |

---

## Sprint 3 — ✅ COMPLETE

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description |
|---------|------|----------|--------|--------|------|-------------|-------------|
| BL-023 | Smart Overdue Notifications | 🟡 Medium | ✅ Done | Sprint 3 | Feature | Agent 1 | Added overdue-contact notification (45-day threshold) to notificationService.ts; skips contacts covered by birthday reminders; i18n EN+HE |
| BL-026 | celebrationType field on Contact | 🟢 Low | ✅ Done | Sprint 3 | Feature | Foundation Agent | CelebrationType type + Contact field; 5-value select in ContactForm Cultural section; CELEBRATION_TO_RELIGION map in scoringSystem filters holiday suggestions; i18n EN+HE |
| BL-030 | Import from device contacts | 🟡 Medium | ✅ Done | Sprint 3 | Feature | Agent 1 | @capacitor-community/contacts installed; DeviceContactsScreen with consent/permission/list/import flow; graceful web fallback; phone dedup; Premium gate; route /device-contacts; shortcut card in ImportContactsScreen |

---

## Sprint 7 — ✅ COMPLETE

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description |
|---------|------|----------|--------|--------|------|-------------|-------------|
| BL-036 | Demo mode with sample data | 🟢 Low | ✅ Done | Sprint 7 | Feature | Agent 1 | `src/data/demoData.ts`: 6 sample contacts (Sarah Cohen/Ahmed Al-Rashid/David Levi/Maria García/Yosef Mizrahi/Nadia Khalil) + 2 groups (Jewish Holidays, VIP Clients). Storage routing: `isDemoMode()/enableDemoMode()/clearDemoMode()` in `storageService.ts`; getContacts/saveContacts/getGroups/saveGroups routed to `nm_demo_*` keys when active. AppContext: `isDemoMode`, `enableDemo`, `clearDemo` exposed. OnboardingScreen: "Try Demo" button on last slide. App.tsx: `DemoBanner` fixed at top (gradient purple) with Exit button. SettingsScreen: "Exit Demo Mode" card when isDemoMode. |
| i18n-S7 | i18n: BirthdayCenterScreen + bonus fixes | 🔴 High | ✅ Done | Sprint 7 | Improvement | Agent 1 | 20 new keys EN+HE: `birthday_heroSubtitle/contactCount/noneTracked/todayBadge/itsTheirBirthday/day/days`, `contacts_addContact`, `calendar_prevMonth/nextMonth`, `groups_searchHolidays`, `dashboard_isTomorrow/prepareGreetings`, `demo_banner/loadData/clear/clearConfirm/cleared`. Fixed 7 strings in BirthdayCenterScreen + 6 across Dashboard/Contacts/Calendar/Groups/HolidayDetail. |

---

## Sprint 8 — ✅ COMPLETE

**Focus: Stability · Accessibility · RTL/Hebrew Correctness**

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description |
|---------|------|----------|--------|--------|------|-------------|-------------|
| F28D | FINDING 28D: GreetingRow RTL dir detection | 🔴 High | ✅ Done | Sprint 8 | Bug | Agent 1 | `dir()` function added to `src/i18n/index.ts` (RTL_LANG_CODES Set: he/ar/fa/ur/yi/dv). `GreetingRow` gains `langCode` prop; `dir` attribute uses `dir(langCode)` instead of fragile string comparison. 4 call sites pass `'he'`/`'ar'`/`'en'` literals. |
| F29A | FINDING 29A: ContactFormScreen Avatar Color label | 🟡 Medium | ✅ Done | Sprint 8 | Bug | Agent 1 | Added `contactForm_avatarColor` key (EN: 'Avatar Color' / HE: 'צבע אווטאר') to i18n. Replaced hardcoded `"Avatar Color"` with `{t('contactForm_avatarColor')}`. |
| F29B | FINDING 29B: ContactFormScreen Auto gradient aria-label | 🔴 High | ✅ Done | Sprint 8 | Bug | Agent 1 | Added `contactForm_autoGradient` key (EN: 'Auto gradient' / HE: 'גרדיאנט אוטומטי') to i18n. Replaced `title="Auto gradient"` with `aria-label={t('contactForm_autoGradient')}` — fixes WCAG 4.1.2. |
| F29C | FINDING 29C: PremiumFeaturePrompt feature prop | 🟡 Medium | ✅ Done | Sprint 8 | Bug | Agent 1 | Already fixed: `feature={t('premium_feat_birthday_fields')}` confirmed in code. No change required. |

---

## Sprint 4+ — Future

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description | Definition of Done |
|---------|------|----------|--------|--------|------|-------------|-------------|-------------------|
| BL-024 | Haptic feedback | 🟢 Low | ✅ Done | Sprint 4 | Feature | Agent 1 | Capacitor Haptics on key actions: save contact, send greeting, coupon success | Haptic fires on target actions; graceful no-op in web browser |
| BL-025 | Export contacts to CSV | 🟡 Medium | ✅ Done | Sprint 4 | Feature | Agent 1 | Premium feature: export all contacts to CSV file; reverse of import | CSV downloadable; all Contact fields included; Premium gate |
| BL-027 | greetingService.ts refactor | 🟢 Low | ✅ Done | Sprint 4 | Technical Debt | Agent 1 | `HOLIDAY_SPECIFIC_BODIES` already in `src/data/greetingTemplates.ts` — refactor was already done | Already done, verified by inspection |
| BL-028 | date-fns named imports | 🟢 Low | ✅ Done | Sprint 4 | Performance | Agent 1 | All date-fns imports verified as named — already correct | Already done, verified by inspection |
| BL-029 | Tap target size WCAG 2.5.5 | 🟡 Medium | ✅ Done | Sprint 4 | Accessibility | Agent 1 | 9 icon-only buttons upgraded: Navigation, Modal, ContactDetail, ContactForm, GroupsScreen, GreetingEditor, HolidayDetail, BirthdayGreetingEditor | All audited icon-only buttons ≥ 48px; no layout regressions |
| BL-034 | Backup / restore JSON | 🟢 Low | ✅ Done | Sprint 4 | Feature | Agent 1 | Full app data export to JSON; restore from file; useful before cloud sync | Export/import cycle preserves all contacts, groups, drafts |
| BL-035 | Duplicate contact detection | 🟢 Low | ✅ Done | Sprint 4 | Feature | Agent 1 | On manual add: detect duplicates by name + phone similarity; Modal offers Save Anyway or Cancel | Detected on new contact; HE UI for decision |
| BL-031 | Supabase v2 integration | 🟡 Medium | 📋 Backlog | Sprint 5 | Feature | Agent 1 | Replace localStorage with Supabase cloud sync; stub already prepared in `src/integrations/supabase/client.ts` | Auth + CRUD working; data migration from localStorage; offline fallback |
| BL-032 | PayMe payment integration | 🟡 Medium | 📋 Backlog | Sprint 9 | Feature | Agent 1 | Real payment flow via PayMe; stub at `src/integrations/payment/payme.ts` | Payment page renders; checkout flow in Hebrew; receipt/confirmation |
| BL-033 | AI greeting suggestions (template-based MVP) | 🟡 Medium | 📋 Backlog | Sprint 10 | Feature | Agent 1 | Premium-only "AI Suggestions" button in GreetingEditorScreen. Generates 2–3 distinct greeting options using deterministic templates keyed by greeting tone (VIP / Professional / Casual) + holiday context. No external API — simulates AI value through variation and tone-awareness. User picks one option; it fills the message field. Future sprint will wire to real Claude API when ready. Stub at `src/integrations/ai/claudeClient.ts` remains untouched. | (1) Button visible in GreetingEditorScreen, premium-gated (free users see lock/upsell); (2) Clicking generates exactly 2–3 options, each meaningfully different by tone; (3) Options vary when a holiday is selected vs. generic reach-out; (4) Selecting an option populates the message field; (5) All option text in Hebrew when language=he; RTL correct; (6) No real API call — fully deterministic; (7) Build + lint pass; no regressions |
| BL-036 | Demo mode with sample data | 🟢 Low | ✅ Done | Sprint 7 | Feature | Agent 1 | 6 sample contacts + 2 groups in demoData.ts; storage routing via nm_demo_* keys; Try Demo button on Onboarding last slide; DemoBanner with Exit; Clear Demo in Settings; real data fully isolated | Sample data loads; DemoBanner shown; Exit clears demo data; real data untouched |
| BL-037 | Contact relationship map | 🟢 Low | 📋 Backlog | Sprint 5 | Feature | Agent 1 | Visual graph of relationships and shared holidays (nice-to-have for v2) | Graph renders; zoomable; RTL-aware layout |
| BL-038 | Recurring greeting scheduler | 🟢 Low | 📋 Backlog | Sprint 5 | Feature | Agent 1 | Schedule a reminder to re-send greeting for same holiday next year | Reminder set; notification fires next year; Hebrew UI |
| BL-046 | Move Import Contacts entry point to Contacts tab | 🔴 High | ✅ Done | Sprint 15 | UX | Agent 1 | Import Contacts primary CTA should live in Contacts tab (not just Settings); current ImportContactsScreen already exists; add visible entry in ContactsScreen (e.g. empty-state CTA or header button) | Import entry visible in Contacts tab; existing premium gate preserved; no duplicate screens |
| BL-047 | Clarify import formats — CSV-only label | 🔴 High | ✅ Done | Sprint 15 | UX | Agent 1 | ImportContactsScreen currently does not communicate supported formats; show: "כרגע ניתן לייבא קובץ CSV בלבד" near the upload area | Hebrew format label displayed; no format confusion |
| BL-048 | Device contacts import visibility in ImportContactsScreen | 🔴 High | ✅ Done | Sprint 15 | UX | Agent 1 | The shortcut card to /device-contacts was added to ImportContactsScreen (Sprint 3) but may not be prominent enough; ensure it is clearly visible and labeled | Device contacts import card visible at top of ImportContactsScreen; shortcut works end-to-end |
| BL-049 | Improve active tab indication in BottomNav | 🟡 Medium | ✅ Done | Sprint 18 | UX | Agent 1 | Active tab icon/label distinction may be subtle; increase contrast or add filled-icon style for active state | Active tab clearly distinguishable at a glance; no regression on theme support |
| BL-050 | Assign imported contacts to a group | 🟡 Medium | ✅ Done | Sprint 5 | Feature | Agent 1 | Implemented in ImportContactsScreen "done" step: group selector + assign button; updateGroup merges imported contactIds; shown only when contacts were imported; Hebrew UI | Group assignment step available post-import; existing/new/none options; Hebrew UI |
| BL-051 | Import/Export Infrastructure Layer — CSV + Excel + localized columns + normalization pipeline | 🟡 Medium | 📋 Backlog | Sprint 5+ | Feature | Agent 1 | Full import/export infrastructure: (1) Downloadable Excel (.xlsx) + CSV templates with all Contact field columns; (2) Localized column headers in both EN and HE; (3) Language/Religion drilldown compatibility — values map to stable internal keys via `src/data/contactConfig.ts`; (4) Normalization pipeline covering all importable fields (birthday, hebrewBirthday, celebrationType, gender, religion, language); (5) Export round-trip: every exported field can be re-imported without data loss. Consolidates CSV and Excel paths under a single import/export service layer. Extends BL-076 (Hebrew birthday import), BL-079 (language-religion drilldown), BL-092 (celebrationType round-trip). Template download is free; device import remains Premium. | Excel + CSV templates download with all field headers; localized columns EN+HE; language-religion drilldown works in Excel template; normalization handles all edge cases; round-trip fidelity verified for all Contact fields; single service layer — no duplicated logic; build+lint pass |
| BL-052 | Smart holiday suggestions based on group purpose | 🟡 Medium | ✅ Done | Sprint 5 | Improvement | Agent 1 | GroupsScreen: purpose selector (7 types), applyPurpose() prunes old + adds new suggestions via SUGGESTED_BASES lookup, ✦ badge on suggested holidays; existing holidays preserved via Set merge | Group purpose selectable; relevant holidays auto-suggested; user can edit; existing holidays preserved; no duplicates; Hebrew/RTL; build passes; no regressions |
| BL-059 | Premium holiday reminder notifications | 🟡 Medium | 📋 Backlog | Sprint 9 | Feature | Agent 1 | Premium-only: when a contact has an upcoming holiday, user can configure reminder channels — Push, SMS, Email, WhatsApp. Per-channel toggle per event type. Duplicate prevention: same contact + same holiday pair never fires twice (tracked via localStorage). Free users see locked state via existing `PremiumFeaturePrompt` upsell flow. No provider integration in this sprint — UI + state logic only. | Channel toggles work per contact+holiday; dedup prevents double-fire; free users see premium lock; UI and state fully in Hebrew; build+lint pass |
| BL-062 | BUG: Upgrade screen CTA provider-specific text + unclear hero title | 🔴 High | ✅ Done | Sprint 10 | Bug | Agent 1 | CTA button text said "שלם עם PayMe" (provider mention — violates no-provider rule). Hero title said "פתח הכל" (unclear). Fix: add generic `upgrade_cta` i18n key (EN: 'Upgrade Now' / HE: 'שדרג עכשיו'); change `upgrade_unlockEverything` HE to 'שדרג לפרמיום'. | CTA uses generic text; no provider name visible; hero title clearly says "שדרג לפרמיום" in Hebrew; build+lint pass |
| BL-063 | BUG: Greeting type not applied + incorrect Hebrew grammar | 🔴 High | ✅ Done | Sprint 11 | Bug | Agent 1 | (1) Switching VIP/Professional/Casual tier does NOT auto-regenerate the message — `tone` state updates but displayed text stays stale; user must manually re-click Generate to see the change. (2) Hebrew `friendly` body templates contain unnatural/grammatically incorrect phrasing (e.g. `"לברר מה שלומך"` — should be `"לאחל"` / `"לשאול"`); full template review needed. Fix must NOT use AI API — stub/templates only, no hardcoded text in components, changes through i18n/templates. | Switching tier immediately updates output; Hebrew phrasing is natural and grammatically correct; no duplicate templates; RTL intact; build+lint pass |
| BL-060 | Special secular and national dates in holiday calendar | 🟡 Medium | ✅ Done | Sprint 29 | Feature | Agent 1 | Add national and secular dates to holidays.ts and the calendar system: יום השואה (Yom HaShoah), יום הזיכרון (Yom HaZikaron), יום השואה הבינלאומי (Jan 27), יום העצמאות של ארה"ב (Jul 4), יום הזיכרון של ארה"ב (last Monday of May), plus any other relevant national/secular dates already in scope. All must appear under the Secular/National filter category (religion: Secular). RTL and Hebrew display required. | All dates appear in calendar with correct dates; filter shows them under Secular/National; Hebrew names render RTL correctly; no regression on existing holidays |
| BL-061 | Religion descriptions for each supported religion | 🟢 Low | 📋 Backlog | — | Feature | Agent 1 | Add a short 1–2 sentence description for each religion in the system (Judaism, Islam, Christianity, Druze, Buddhism, Hinduism, Sikhism, Bahá'í, East Asian, Secular), covering: what it is, who observes it, and relevant cultural context for greeting purposes. Display in religion selector or info tooltip in ContactFormScreen and/or CalendarScreen. Hebrew and English versions required; RTL support required. | Descriptions visible in UI for all 10 religions; EN+HE versions present; RTL correct; no regression on existing religion selectors |
| BL-064 | One-tap greeting send flow | 🔴 High | ✅ Done | Sprint 13 | Feature | Agent 1 | When a contact has an upcoming holiday or birthday, surface an inline "Send Greeting" CTA directly from the reminder/dashboard alert. User taps → sees 2–3 pre-generated greeting options (from aiSuggestionsService) → selects one → opens the selected channel (WhatsApp/SMS/Email) pre-filled. Goal: minimize steps from "reminder received" to "greeting sent" to near-zero. No automated sending — user still confirms in the channel app. MVP: no push notifications integration; trigger from Dashboard alert cards. | Greeting CTA visible on dashboard holiday/birthday alert cards; tapping shows options panel; selecting opens pre-filled channel; no extra navigation; build+lint pass |
| BL-128 | Feature: Help section + support request intake | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | In-app Help section accessible from all screens (dismissible X button; permanent section in Settings). Authentication is NOT required — all users including anonymous can submit. Form: one text field (description) + optional email field. Anonymous users can omit email entirely. Requests stored in Supabase `support_requests` table (fields: id, created_at, user_email nullable, description, app_version, language). If user is authenticated, email pre-filled from auth session. If anonymous, email is optional and user-provided. Optional future: Excel/CSV export for operational use — not MVP scope. Aligns with anonymous-first MVP philosophy: zero friction for all users. RLS note (future): anonymous users must have INSERT-only access to `support_requests` — no SELECT. Authenticated users also INSERT-only. No row should ever be readable by the submitting client. BLOCKED by BL-031 (Supabase data layer). EN+HE RTL. | Help button visible on all screens; permanent section in Settings; anonymous users can submit without login; email field optional; authenticated user email pre-filled; request stored in Supabase; RLS enforced (INSERT-only, no SELECT for any client); EN+HE; RTL correct; build+lint pass |
| BL-101 | Feature: Contact Discovery — shared cultural profile (opt-in) | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | When a database exists, allow users to opt-in to sharing their cultural profile so others can auto-fill it when adding them as a contact. Flow: (1) During registration, user chooses "Allow others to auto-fill my cultural details when adding me as a contact" (default OFF). (2) When another user adds a contact by phone (primary) or email (secondary) — name alone is never used for matching — if the contact has opted in, auto-fill: Religion, Birthday (Hebrew + Gregorian), Language preference, celebrationType. Fields never shared: Notes, Groups, Relationship type, any personal/internal data. Privacy: opt-in only; user can change preference anytime; no data shared without explicit consent; shared data is read-only for importing user; auditability (who shared what, when) is a future requirement. Identity matching: phone is primary identifier, email is secondary — prevents accidental mismatches. Requires: Supabase (or equivalent), user authentication, Row Level Security (RLS), user identity layer. BLOCKED by database + auth migration epic — do NOT start before DB + Auth exist. | Opt-in toggle exists during registration and in settings; auto-fill works via phone/email match only; shared fields are correct subset; private fields not exposed; opt-out stops future sharing; no data shared without consent; RLS enforced; build+lint pass |
| BL-099 | Improvement: Contact quick-add shortcut (name + phone only) | 🟡 Medium | ✅ Done | Sprint 26 | Improvement | Agent 1 | The full contact form has 10+ fields. New users face high friction when trying to add their first contact. Add a `QuickAddModal` bottom sheet (name + phone required) triggered from empty state CTAs on Dashboard and ContactsScreen. On save: creates a contact with sane defaults. Link to full form for users who want to add more details. No navigation required — modal closes and user sees their first contact immediately. | Quick-add modal opens from empty state CTA; name and phone are required; contact saved on submit; modal closes and contact appears; link to full form present; RTL correct; build+lint pass |
| BL-098 | Improvement: Empty state screens with value messaging (Dashboard + Contacts) | 🟡 Medium | ✅ Done | Sprint 26 | Improvement | Agent 1 | Dashboard and ContactsScreen empty states use generic placeholder text ("Start adding contacts"). Update messaging to be emotionally resonant and value-driven: make the user think about people they care about and give them a reason to act. CTA triggers `QuickAddModal` (BL-099) instead of navigating to full form. Groups screen excluded from this sprint. | Empty states show emotional/value-driven messaging; CTA triggers quick-add modal; no regression in existing contact flow; RTL correct; build+lint pass |
| BL-100 | BUG: Greeting contact dropdown includes placeholder as selectable option | 🟡 Medium | ✅ Done | Sprint 27 | Bug | Agent 1 | In `GreetingEditorScreen`, the contact dropdown (`בחר איש קשר`) renders the placeholder `— בחר איש קשר —` as a selectable `<option>` inside the open list. The placeholder should be a display-only label (not selectable), visible only as the empty field value before the user makes a selection. Fix: add `disabled` + `hidden` attributes to the placeholder option, or use `value=""` with a guard so it cannot be selected. Minimal change — do not refactor the dropdown component. | Contact dropdown shows only real contacts when opened; placeholder is not selectable; empty/pre-selection state still shows placeholder text as the field label; no regression in contact selection logic; RTL correct; build+lint pass |
| BL-097 | Feature: Landing page + login preview flow for MVP | 🟡 Medium | ✅ Done (Landing page) | Sprint 28 | Feature | Agent 1 | Pre-login experience before the app. (1) Landing page: clear value proposition (don't miss important moments), emotional positioning, relationship imagery (romantic couple, family, friends, work colleagues). Page should drive action — not just inform. CTA buttons: Login / Register / Enter demo. (2) Guided feature walkthrough: 5 steps (Add contacts → Organize into groups → Get reminders → Generate/send greetings → Import/export). Each step individually skippable; "Skip all" option; "Continue to login" at any point. (3) Login placeholder: email input + "Continue"/"Enter demo" buttons; no real auth; optional note "Authentication coming soon". (4) Returning user behavior: after onboarding, user can choose Skip or "Do not show again". localStorage flags: `hasSeenOnboarding` (boolean) and `dontShowLanding` (boolean). First visit → show landing + walkthrough. Returning user → skip automatically if `dontShowLanding` is true. EN+HE RTL. No new dependencies. No database. No real authentication. Must not block access to current app. | Landing page exists before app entry; includes emotional messaging + relationship imagery; CTA has Login/Register/Enter demo buttons; walkthrough supports per-step skip + skip all; "Do not show again" sets dontShowLanding flag; returning user auto-skips if flag set; login placeholder exists; RTL correct; build+lint pass |
| BL-096 | Improvement: Assign/remove group when editing existing contact | 🔴 High | ✅ Done | Sprint 25 | Improvement | Agent 1 | Group picker in `ContactFormScreen` is limited to new contacts only (`{isNew && groups.length > 0}` guard, lines ~501–514). Edit mode has no group assignment UI. Extend the group picker to work in both create and edit mode: show current group memberships on load, allow toggling. On save in edit mode, call `updateGroup()` to add/remove the contact ID from the relevant groups' `contactIds` arrays (same logic as new contact path). | Group picker visible in edit mode; pre-selected based on current memberships; adding/removing a group saves correctly; new contact flow unchanged; EN+HE; build+lint pass |
| BL-095 | Feature: Contact gender field for greeting personalization | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | Add optional `gender` field to `Contact` type (`'male' \| 'female' \| 'other' \| undefined`). Add gender selector (3-option toggle or select) in `ContactFormScreen` under the Personal section. Use the `gender` field in `greetingService.ts` to adjust pronouns and message phrasing (e.g. Hebrew has grammatical gender). CSV export/import round-trip must include the field. EN+HE labels required. | gender field on Contact type; selector in ContactFormScreen; greetingService uses gender for pronoun/phrasing; CSV export includes field; import maps it correctly; EN+HE; build+lint pass |
| BL-094 | BUG: Holiday names always display in English regardless of app language | 🔴 High | ✅ Done | Sprint 25 | Bug | Agent 1 | `DashboardScreen` and `HolidayCard` render `holiday.name` directly (English-only field). Hebrew users see English holiday names across the entire app. `getHolidayDisplayName()` already exists in `GroupsScreen` (checks `lang === 'he'`, picks Hebrew alternative from `alternativeNames`) but is scoped locally and not shared. Fix: extract `getHolidayDisplayName()` to a shared utility; apply it to all holiday name render points: DashboardScreen (lines 203, 347), HolidayCard (lines 54, 94), and any other component rendering `holiday.name`. | Holiday names display in the selected app language; Hebrew users see Hebrew names; shared utility in place; applied to all render sites; no regression on English mode; build+lint pass |
| BL-093 | Improvement: Group purpose UX + religion-based holiday filter — Phase 1 done | 🟡 Medium | ✅ Done (Phase 1) | Sprint 24 | Improvement | Agent 1 | Phase 1: group_purpose_hint copy improved (EN: "Pre-selects relevant holidays" / HE: "מסייע לבחור חגים רלוונטיים"); religion filter chips derived from uniqueHolidays added above holiday search — display-only filter, no auto-selection. Phase 2 (auto-selection) deferred to future sprint. | Phase 1: purpose copy clearer; religion filter present; filter works; no auto-selection; build+lint pass |
| BL-092 | Bug/Gap: celebrationType round-trip broken — exported but not imported | 🟡 Medium | ✅ Done | Sprint 22 | Bug | Agent 1 | Added celebrationType to DETECTABLE_FIELDS with patterns (celebration type/celebration/סוג חגיגה); normalizeCelebrationType() maps common values to CelebrationType enum; processImport writes celebrationType to Contact; round-trip export↔import now preserves celebration type | celebrationType exported and re-imported correctly; field present on Contact after import; build+lint pass |
| BL-091 | Gap: Export warning — group assignments not included in CSV | 🟡 Medium | ✅ Done | Sprint 22 | Improvement | Agent 1 | Added settings_exportGroupWarning i18n key (EN+HE); warning note rendered below CSV export button in SettingsScreen informing user that group assignments are not included in the exported file | Warning visible in Settings below export button; EN+HE; build+lint pass |
| BL-090 | Gap: hebrewBirthday no format validation in import or display | 🟡 Medium | ✅ Done | Sprint 22 | Bug | Agent 1 | Added normalizeHebrewBirthday() in importService.ts — validates DD-MM format, returns undefined for invalid values; added regex guard /^\d{1,2}-\d{1,2}$/ in ContactDetailScreen.tsx before displaying hebrewBirthday | Malformed hebrewBirthday strings not stored on import; invalid values not shown in contact detail; build+lint pass |
| BL-089 | Gap: Group membership not visible in contacts flow | 🟡 Medium | ✅ Done | Sprint 24 | Improvement | Agent 1 | ContactCard: chips show up to 2 groups (emoji + name, colored) with +N overflow. ContactDetailScreen: Groups section shows all groups contact belongs to. Reverse-lookup via Group.contactIds. useMemo before early return. EN+HE. | Group membership chip visible on ContactCard and ContactDetailScreen; accurate; RTL; build+lint pass |
| BL-088 | BUG: ContactsScreen subtitle shows "groups" instead of "contacts" count | 🔴 High | ✅ Done | Sprint 23 | Bug | Agent 1 | ContactsScreen.tsx line 90: subtitle uses t('groups_group') / t('groups_groups') keys instead of contact-specific keys. In Hebrew, this shows "5 קבוצות" on the contacts screen. Fix: use t('contacts_contact') / t('contacts_contacts') or equivalent correct keys. | Contacts subtitle shows correct noun (contact/contacts); Hebrew text correct; build+lint pass |
| BL-087 | Gap: Active group filter in ContactsScreen has no UI indicator or clear action | 🟡 Medium | ✅ Done | Sprint 23 | Improvement | Agent 1 | When navigating from GroupsScreen contact count badge to /contacts?group=X, ContactsScreen filters silently with no indication. User sees fewer contacts with no explanation and no way to clear filter. Add a visible filter chip/banner and a clear (×) button. | Active filter chip visible when ?group= param is active; clear button removes filter; no filter = full list; RTL; build+lint pass |
| BL-086 | Gap: Group delete does not clean up contact references | 🔴 High | ✅ Done | Sprint 23 | Bug | Agent 1 | deleteGroup() removes the group record but does not iterate contacts to remove the deleted groupId from group.contactIds. No crash, but referential integrity is broken. Fix: on deleteGroup, call updateGroup or equivalent for all contacts that were in the deleted group (or implement cleanup in storageService). | Deleting a group no longer leaves orphaned contactIds; referential integrity maintained; build+lint pass |
| BL-085 | Improvement: Upcoming events list not sorted by proximity | 🟡 Medium | ✅ Done | Sprint 25 | Improvement | Agent 1 | Upcoming events are displayed out of order — an event 18 days away can appear before an event 2 days away. Fix: locate where the upcoming events list is assembled (do NOT assume file name), then apply an ascending sort by `daysUntilEvent` before rendering. Rules: do NOT change event calculation logic; do NOT modify UI; sorting only; minimal change. Same-day events keep stable relative order. Past events must not appear, or appear at bottom. | Closest event always appears first; same-day events stable; past events absent or at bottom; no regression in event calculation; build+lint pass |
| BL-084 | BUG: Imported contact opens error page when clicked | 🔴 High | ✅ Done | Sprint 21 | Bug | Agent 1 | normalizeBirthday() added to importService.ts; isNaN guard added to ContactDetailScreen.tsx; prevents RangeError crash on non-ISO birthday strings imported from CSV | Imported contacts open correctly in ContactDetailScreen; no error page; imported contact object matches shape of manually-created contact; id present and valid; build+lint pass |
| BL-083 | Feature: First-time onboarding + feature guide | 🟡 Medium | ✅ Done | Sprint 27 | Feature | Agent 1 | New users enter the app with no explanation of product purpose or feature guidance. Add: (1) First-time onboarding — 2–3 lightweight slides explaining what NeverMiss is, what problem it solves, and how to start (add a contact, set a reminder, send a greeting). Show only on first launch; skippable; uses existing OnboardingScreen infrastructure if applicable. (2) Feature guide — short walkthrough of core features: Contacts, Groups, Reminders, CSV Import. Accessible via "View Guide" in Settings or Dashboard. (3) Behavior: onboarding shown once; guide re-accessible anytime; never blocks user flow. UX: simple non-technical language, Hebrew RTL, mobile-friendly. No Premium dependency. | Onboarding shown on first launch only; skippable; guide accessible from Settings or Dashboard; covers Contacts / Groups / Reminders / CSV Import; Hebrew RTL correct; no blocking of user flow; build+lint pass |
| BL-082 | Improvement: Points system unclear — missing explanation and definition | 🟡 Medium | ✅ Done | Sprint 21 | Improvement | Agent 1 | Score badge in ContactCard converted to tappable button; tooltip shows score_tipTitle + score_tipBody in EN+HE; click-outside and Escape close tooltip | Score explanation visible on tap/hover; explains earning logic in simple Hebrew and English; RTL correct; no regression on score calculation; build+lint pass |
| BL-081 | BUG: RTL directional icons pointing wrong way in ImportContactsScreen | 🔴 High | ✅ Done | Sprint 18 | Bug | Agent 1 | 3 hardcoded `ArrowRight` icons in `ImportContactsScreen.tsx` (lines 188, 292, 334) are not RTL-aware. In Hebrew mode, directional icons should mirror to point left. Root cause: icon choices are unconditional `<ArrowRight />`, not checking language direction. Fix: replace each with `lang === 'he' ? <ArrowLeft /> : <ArrowRight />` conditional. All other screens already RTL-correct (Navigation, CalendarScreen, OnboardingScreen, SettingsScreen each use `lang === 'he'` conditional). | All directional icons in ImportContactsScreen point correctly per language; no hardcoded unidirectional arrow; Hebrew RTL correct; English LTR correct; build+lint pass |
| BL-080 | BUG: Incorrect notification timing for Israeli holidays (Yom HaZikaron, Yom HaAtzmaut) | 🔴 High | ✅ Done | Sprint 17 | Bug | Agent 1 | Notifications fired on the wrong date for Yom HaZikaron and Yom HaAtzmaut. Root cause suspected: Hebcal not using Israel-specific rules, or timezone not set to Asia/Jerusalem. Fix: (1) Verify @hebcal/core is called with Israel mode (`il: true`). (2) Verify timezone handling uses Asia/Jerusalem throughout notification scheduling. (3) Ensure notification triggers only on the confirmed correct date/time per Hebcal Israel output. (4) Add safeguard: if holiday date is missing or uncertain, do not send notification. (5) Do not hardcode dates. | Notifications fire on correct Israeli date for Yom HaZikaron and Yom HaAtzmaut; Asia/Jerusalem timezone applied; missing-date safeguard in place; no hardcoded dates; build+lint pass |
| BL-075 | UX: Replace visible edit/delete actions on contact rows with 3-dot overflow menu | 🟡 Medium | ✅ Done | Sprint 18 | Improvement | Agent 1 | In Contacts, each contact currently shows direct edit/delete action buttons, cluttering the UI. Replace with a 3-dot (⋯) overflow menu button. On click, show a small menu with two items: "עריכה" (Edit) and "מחיקה" (Delete). Implementation: (1) Find the contact list/card component. (2) Replace visible edit/delete buttons with a 3-dot overflow button. (3) Show menu on click with Edit and Delete items. (4) Delete must still use existing confirmation flow. (5) Keep existing edit/delete logic unchanged. (6) Menu works in Hebrew/RTL and mobile. (7) Accessibility: aria-label on 3-dot button; menu items keyboard accessible; close on outside click or Escape key. Do NOT change contact data logic. Minimal UI change only. | 3-dot button visible on each contact row; clicking opens menu with Edit and Delete; Delete triggers existing confirmation flow; menu accessible via keyboard; RTL-correct; build+lint pass |
| BL-079 | Improvement: Language-Religion drilldown + CSV localization (single source of truth) | 🟡 Medium | ✅ Done | Sprint 19 | Improvement | Agent 1 | Import template requires manual typing of Language and Religion — causing spelling errors. Also: (1) CSV example row shows raw keys ("hebrew", "Judaism") instead of localized values. (2) Religion dropdown is not dependent on selected language. Fix: (a) Add Excel dropdown validation for Language (all app-supported languages, localized). (b) Religion dropdown dependent on selected language — values appear in selected language (יהדות/Judaism) but map to stable internal keys. (c) Implement DRILLDOWN: selecting Language updates Religion options. (d) Create single shared config/enum for Language+Religion used in CSV template, UI dropdowns (ContactFormScreen), and import parser — no value duplication across files. (e) CSV example row shows localized values matching selected language. Existing typed imports must still work. Use existing i18n religion list; do not hardcode. | Language dropdown localized; Religion dropdown language-dependent with drilldown; CSV example row shows localized values; same config used in UI + CSV + import; existing imports unaffected; no duplicated logic; build+lint pass |
| BL-078 | UX: Fix misleading "mobile required" message on mobile browser for device contacts import | 🟢 Low | 📋 Backlog | — | Improvement | Agent 1 | DeviceContactsScreen shows "תכונה זו מצריכה אפליקציית מובייל" when accessed on a mobile browser; this is misleading since the user IS on mobile — the real constraint is native app. Fix: detect mobile-browser-not-native case and show "זמין רק באפליקציה — יש להוריד את האפליקציה כדי להשתמש בתכונה זו" + "התקן אפליקציה" CTA (trigger PWA install prompt if available, else no-op / future store redirect). Feature remains blocked on web. No refactor, no permission logic change — UX copy and CTA only. | Correct message shown on mobile browser (not "מצריכה מובייל"); CTA button present; feature still blocked on web; build+lint pass |
| BL-077 | Tech Debt: Replace static holiday entries with dynamic Hebcal-driven source | 🔴 High | ✅ Done | Sprint 14 | Bug/Tech Debt | Agent 1 | The current `holidays.ts` hardcodes 2025–2026 date entries for all holidays including the newly added Jewish minor holidays and fast days (BL-074). This is not maintainable — every year requires manual updates. Replace the static year-specific entries with a fully dynamic approach: at runtime, use `@hebcal/core` (already a project dependency) to generate holiday dates for any requested year range. Support all years, all holiday types (major, moderate, minor, fast, cultural, national), and all religions. Remove hardcoded `date` values for Hebrew-calendar holidays entirely. Static Gregorian dates (Christmas, US holidays, etc.) may remain static. The BL-074 static entries added in Sprint 13 should be treated as temporary fallback only and replaced by this implementation. | Dynamic generation for all Hebrew-calendar holidays across any year; no hardcoded 2025/2026 date values for Hebrew holidays; minor holidays and fast days included; existing holiday IDs and structure preserved or cleanly migrated; no manual date maintenance required; build+lint pass |
| BL-076 | Improvement: CSV import template missing Hebrew birthday date support | 🟡 Medium | ✅ Done | Sprint 20 | Improvement | Agent 1 | Hebrew Birthday (DD-MM) column added to import template (EN+HE); hebrewBirthday added to DETECTABLE_FIELDS (ordered before birthday to prevent false match); processImport writes hebrewBirthday to Contact; export↔import round-trip complete | Hebrew birthday columns present in template; fields are optional; import maps to hebrewBirthday; existing Gregorian birthday imports unaffected; Hebrew format instructions in template; build+lint pass |
| BL-065 | AI greeting generation — real Claude API integration | 🔴 High | 📋 Backlog | — | Feature | Agent 1 | Replace deterministic template engine (BL-033) with real Claude API via `src/integrations/ai/claudeClient.ts` stub. Architecture direction: (1) **Relationship-aware prompts** — contact's relationship type (family/friend/manager/client/VIP), last interaction date, and personal notes shape the prompt; (2) **Contextual personalization** — holiday/birthday context, selected tone (VIP/Professional/Casual), contact's preferred language fed into the prompt; (3) **Human-feeling greetings** — output that reads as genuinely personal and non-templated, avoiding AI boilerplate patterns; (4) **API-based generation** — single Claude API call per request, result cached per `contact.id + holiday.id + tone` to avoid repeat billing; (5) **Graceful fallback** — if API call fails or user is offline, silently fall back to existing deterministic engine; (6) **Cost guard** — configurable daily API call limit per authenticated user (Premium only). Hebrew prompt engineering required: system prompt must instruct Claude to respond in Hebrew when `language === 'he'`. Do NOT implement in stabilization sprints — this item is planned for post-stabilization. BLOCKED by: stable auth layer (BL-125), Supabase user identity (BL-031). | Claude API call fires on "Generate" button press; output is relationship-aware and non-templated; Hebrew output when language=he; fallback to templates on API failure; per-user result cache in place; Premium gate enforced; no regressions on existing greeting flow; build+lint pass |
| BL-066 | Recurring and smart reminder automation | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | Allow users to: (1) mark a holiday/birthday reminder as "remind me every year" — stored in localStorage as a recurring rule; (2) define group-level reminders (e.g. "remind me 7 days before any holiday for my Clients group"); (3) set a custom lead time per VIP contact (e.g. 14 days instead of 7). MVP: localStorage-based rules engine, no background execution — rules are evaluated at app open and surfaced on dashboard. Push notification scheduling (Capacitor) is a stretch goal. Premium-gated for group rules and VIP lead times; Hebrew/RTL required. | Yearly recurrence toggle visible on holiday detail; group reminder rules configurable; VIP lead time customizable; rules evaluate on app open; build+lint pass |
| BL-067 | Personal user layer — first-use setup + personalized entry screen | 🔴 High | ✅ Done | Sprint 16 | Feature | Agent 1 | On first visit, ask the user their name (single input screen, no auth). Store in localStorage. On all subsequent visits, show a personalized entry: "Hey {name} 👋 / יאללה {name} 👋" + "You have X greetings to send today" / "שלחת ברכה ל-X אנשים החודש". If no upcoming events: "Let's send your first greeting" / "בוא נשלח את הברכה הראשונה שלך". Context panel on dashboard: upcoming events (next 7 days), pending greetings (contacts with birthday/holiday within 3 days not yet greeted), recent contacts (last 3 greeted). No auth, no backend — localStorage only. Hebrew/RTL required. | Name prompt shown on first launch only; name persisted across sessions; personalized greeting shown on entry; context panel visible on dashboard; empty states handle no-data gracefully; build+lint pass |
| BL-068 | Add contacts to group from group screen | 🟡 Medium | ✅ Done | Sprint 21 | Feature | Agent 1 | Contact picker added to GroupsScreen create/edit modal; selectedContactIds state; openForm initializes from group.contactIds; handleSave writes contactIds; searchable; EN+HE | Contacts list visible in group form; searchable; checkboxes work; selected contacts saved to group; build+lint pass |
| BL-069 | Assign contact to group when adding a new contact | 🟡 Medium | ✅ Done | Sprint 21 | Feature | Agent 1 | Group selector added to ContactFormScreen (new contacts only); after addContact calls updateGroup to append contactId; EN+HE | Group field visible in contact form; optional (not required); existing groups listed; contact saved with group assignment; build+lint pass |
| BL-070 | ⚠️ TEMP: Re-enable Premium gates before production launch | 🔴 High | 📋 Backlog | pre-launch | Configuration | Developer | Set `TEMP_PREMIUM_UNLOCK = false` (or remove the constant) in `src/context/AppContext.tsx` line ~67. Added 2026-05-01 as part of MVP testing unlock. All premium gates restore automatically. Also remove the `// TEMP` comment. | `TEMP_PREMIUM_UNLOCK` removed or set to `false`; all premium gates enforced; build+lint pass; verified on UpgradeScreen and PremiumFeaturePrompt |
| BL-071 | BUG: WhatsApp sharing does not preserve emojis | 🔴 High | ✅ Done | Bug Fix Mode | Bug | `src/services/communicationService.ts` | Investigated: `buildWhatsAppUrl` already uses `encodeURIComponent(message)` at line 15. All WhatsApp call sites funnel through this function (ChannelPicker, WhatsAppButton, DashboardScreen quick-send). Emoji encoding is correct at all paths. Verified: `encodeURIComponent('🎂 🎉 ❤️ ✨')` produces valid percent-encoded UTF-8 that WhatsApp decodes correctly. No code change required — encoding was already present. | Emojis (🎂 🎉 ❤️ ✨) appear correctly in WhatsApp pre-fill; encodeURIComponent confirmed at all call sites |
| BL-072 | BUG: Voice message flow is unclear — no user guidance after WhatsApp opens | 🟡 Medium | ✅ Done | Bug Fix Mode | Bug | `src/components/ChannelPicker.tsx`, `src/components/WhatsAppButton.tsx`, `src/i18n/index.ts` | WhatsApp does not support audio attachment via URL. When a voice message exists and user taps WhatsApp, the text message opens in WhatsApp but there is no clear instruction to manually attach the audio file. Fixed: added a post-send `Modal` (stays until dismissed, RTL-ready) in both `ChannelPicker` and `WhatsAppButton` that fires when `media.type === 'audio'`. Modal uses i18n keys `whatsapp_voice_hint_title` / `whatsapp_voice_hint_body` (EN+HE). Text flow unchanged — only the audio path shows the hint. | Modal appears after WhatsApp opens when voice media exists; stays until dismissed; Hebrew text correct; text-only flow unchanged; build+lint pass |
| BL-073 | BUG: Duplicate holidays in Greeting Editor Event Type dropdown | 🟡 Medium | ✅ Done | Sprint 13 | Bug | `src/screens/GreetingEditorScreen.tsx` | The Event Type `<Select>` in GreetingEditorScreen showed duplicate holiday names (e.g. Rosh Hashana appeared twice) because HOLIDAYS has both 2025 and 2026 entries with the same `name`. Fixed: added `useMemo`-based deduplication that keeps the most upcoming entry per holiday name. | Each holiday appears once in the dropdown; most upcoming year entry is shown; no functional regression; build+lint pass |
| BL-074 | BUG: Missing Jewish minor holidays and fast days | 🔴 High | ✅ Done | Sprint 13 | Bug | `src/data/holidays.ts` | 7 Jewish observances were missing from the holidays data: Lag Ba'Omer, Simchat Torah, Tisha B'Av, 17th of Tammuz, 10th of Tevet, Fast of Esther, Fast of Gedaliah. These could not be selected in Greeting Editor or shown on the Calendar. Fixed: added 14 new holiday entries (2025+2026 pairs) using `hebcalDate()` for accurate Hebrew-calendar dates. Types: minor (Lag Ba'Omer), major (Simchat Torah), fast (all others). EN+HE descriptions; greetings; color; emoji. | All 7 holidays appear in Event Type dropdown and Calendar; Hebrew names in alternativeNames; fast days use type: 'fast'; minor uses type: 'minor'; build+lint pass |

---

## Sprint 1 — Completed

> Will be archived to the Archive section after Sprint 3.

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description | Definition of Done |
|---------|------|----------|--------|--------|------|-------------|-------------|-------------------|
| BL-S1-01 | BUG-020 DST birthday fix | 🔴 High | ✅ Done | Sprint 1 | Bug | Agent 1 | `getBirthdayDaysUntil` raw ms division returns -1 on DST spring-forward; replaced with `differenceInCalendarDays` | Build+lint pass; DST no longer affects birthday countdown |
| BL-S1-02 | BUG-021 key stability ImportContacts | 🟡 Medium | ✅ Done | Sprint 1 | Bug | Agent 1 | `key={i}` on CSV preview rows and error strings; replaced with `key={\`row-${i}\`}` and `key={e}` | No React reconciliation warnings; stable keys on re-render |
| BL-S1-03 | i18n: urgency/freq/action/go_back/holiday/about keys | 🔴 High | ✅ Done | Sprint 1 | Improvement | Foundation Agent | 39 new i18n keys (urgency_*, go_back, action_*, holiday_major, holiday_moreContacts, about_*) in EN + HE | All keys present in both locales; no fallback-to-key rendering |
| BL-S1-04 | Navigation.tsx go_back translated (FINDING 25A) | 🟡 Medium | ✅ Done | Sprint 1 | Accessibility | Agent 1 | `aria-label="Go back"` hardcoded EN in PageHeader; replaced with `t('go_back')` | HE: aria-label="חזור"; build+lint pass |
| BL-S1-05 | ContactDetailScreen i18n fixes (FINDING 20/23) | 🔴 High | ✅ Done | Sprint 1 | Improvement | Agent 1 | urgencyLevel, interactionFrequency, and suggestedAction.label always shown in EN; replaced with t() + translatedActionLabel | All 3 fields render in HE when language=he |
| BL-S1-06 | HolidayDetailScreen i18n (FINDING 28A) | 🟡 Medium | ✅ Done | Sprint 1 | Improvement | Agent 1 | "★ Major" and "+N more contacts" hardcoded EN; replaced with t('holiday_major') and t('holiday_moreContacts') | Renders in HE; correct counts |
| BL-S1-07 | AboutScreen full i18n (FINDING 27) | 🟡 Medium | ✅ Done | Sprint 1 | Improvement | Agent 1 | VALUES/TECH arrays + mission paragraph all hardcoded EN (16 strings); replaced all with t() | AboutScreen fully HE in Hebrew mode |
| BL-S1-08 | Dashboard/Groups div→button (FINDING 67) | 🔴 High | ✅ Done | Sprint 1 | Accessibility | Agent 1 | Today-highlight cards in Dashboard and group cards in GroupsScreen were `<div onClick>` — not keyboard accessible | Elements are `<button type=button>`; Tab+Enter works |
| BL-S1-09 | .btn:focus-visible rule (FINDING 69) | 🟡 Medium | ✅ Done | Sprint 1 | Accessibility | Agent 1 | `.btn` class lacked `:focus-visible` rule; primary buttons relied on UA default only | `outline: 2px solid var(--color-primary)` on focus; all 6 themes verified |
| BL-S1-10 | Desktop full-width layout | 🔴 High | ✅ Done | Sprint 1 | Bug | Agent 1 | `.app-layout` had `max-width: 480px` — app showed as narrow column on desktop/web | `@media (min-width: 768px)` removes constraint; full viewport on desktop |
| BL-S1-11 | Modal close aria-label (FINDING 30B) | 🟡 Medium | ✅ Done | Sprint 1 | Accessibility | Agent 1 | `aria-label="Close"` hardcoded EN in Modal.tsx; Modal had no useT() | Modal imports useT(); aria-label uses t('close') |
| BL-S1-12 | ImportContactsScreen finally block (FINDING 60) | 🟡 Medium | ✅ Done | Sprint 1 | Bug | Agent 1 | `setImporting(false)` placed after try/catch block, not in finally; import button could stay locked | `setImporting(false)` now in finally block |
| BL-S1-13 | ContactCard aria-hidden icons (FINDING 66) | 🟢 Low | ✅ Done | Sprint 1 | Accessibility | Agent 1 | Crown and Building2 icons in ContactCard missing `aria-hidden="true"`; verbose screen reader output | `aria-hidden="true"` on both icons |

---

## Post-Sprint 30 — Stabilization Backlog (BL-102 to BL-130)

_Added: 2026-05-08 | Covers: auth stability, Hebrew calendar trust, RTL/localization, legal, anonymous-first UX, architecture hardening_

| Task ID | Task | Priority | Status | Sprint | Type | Owner Agent | Description | Definition of Done |
|---------|------|----------|--------|--------|------|-------------|-------------|-------------------|
| BL-102 | BUG: Auth gate flicker — app content briefly visible before session check completes | 🔴 High | 📋 Backlog | Sprint 31 | Bug | Agent 1 | AuthGate renders after Supabase session check resolves, but during the async check window the app shell (or route content) may flash visible. Fix: ensure AuthGate shows a loading skeleton/spinner for the full duration of `isLoading === true`; no partial render of protected content during check. No layout changes — only render-gating. | No app content visible before session check resolves; loading state shown consistently; no regression in auth flow; build+lint pass |
| BL-103 | Improvement: Magic link flow — resend button + 60s cooldown + Hebrew error states | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | After requesting a magic link, user sees a confirmation screen with no way to resend if email doesn't arrive. Add: (1) "שלח שוב" / "Resend" button with 60-second cooldown; (2) countdown timer visible in Hebrew; (3) Hebrew error state if Supabase returns a send failure; (4) "בדוק בתיקיית הספאם" hint. No new dependencies — Supabase auth API already supports resend. | Resend button visible; 60s cooldown enforced; Hebrew countdown shown; error states in Hebrew; spam hint present; build+lint pass |
| BL-104 | Improvement: Session expiry — re-authentication prompt instead of silent redirect | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | When a Supabase session expires mid-use, the user is silently redirected to the login screen with no explanation. Replace with: in-app modal/toast "הפעלה פגה תוקף — יש להתחבר מחדש" with a "התחבר מחדש" CTA. User's current screen context preserved where possible. Anonymous users unaffected. | Expiry prompt shown instead of silent redirect; Hebrew text; CTA triggers re-auth; anonymous users unaffected; build+lint pass |
| BL-105 | Improvement: Logout confirmation dialog with data-persistence note for anonymous users | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | Current logout fires immediately with no confirmation. Add a confirmation dialog: "האם אתה בטוח שברצונך להתנתק?" + "אישור" / "ביטול". For anonymous sessions: add a secondary note "הנתונים שלך נשמרים בדפדפן — ייתכן שיאבדו אם תמחק קובצי עוגיות" to manage expectations. Authenticated users: standard confirmation only. | Confirmation dialog shown before logout; Hebrew text; data note shown for anonymous; Cancel aborts; Confirm logs out; build+lint pass |
| BL-106 | BUG: Auth/settings input fields — text direction not forced to RTL in Hebrew mode | 🔴 High | 📋 Backlog | Sprint 31 | Bug | Agent 1 | Email input on the login/register screen and text inputs in the Settings Account section do not set `dir="rtl"` when `language === 'he'`. Result: Hebrew text typed by the user displays LTR. Fix: apply `dir={lang === 'he' ? 'rtl' : 'ltr'}` on all `<input>` and `<textarea>` elements in auth and settings screens. Consistent with pattern already used in ContactFormScreen. | All auth and settings text inputs have correct `dir` attribute in Hebrew mode; Hebrew text displays RTL; English mode unchanged; build+lint pass |
| BL-107 | Localization: Magic link confirmation and error screens missing Hebrew translations | 🔴 High | 📋 Backlog | Sprint 31 | Improvement | Agent 1 | Screens introduced in Sprint 30 (magic link sent confirmation, auth error, email input) contain hardcoded English strings not yet added to `src/i18n/index.ts`. Audit all strings in auth flow screens; add missing keys in both EN and HE; replace hardcoded text with `t()` calls. RTL layout correct in Hebrew. | All auth-flow strings in `src/i18n/index.ts`; no hardcoded English visible in Hebrew mode; RTL correct; build+lint pass |
| BL-108 | Localization: Settings Account section — plan labels and account action strings hardcoded English | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | Sprint 30 added an Account section to SettingsScreen. Plan name ("Free", "Premium"), account status labels, and action buttons (e.g. "Sign out") may be hardcoded in English. Audit the Account section; add missing i18n keys EN+HE; replace hardcoded strings. | Settings Account section fully localized in Hebrew mode; no hardcoded English strings; build+lint pass |
| BL-109 | Improvement: Dashboard event cards — show Hebrew date alongside Gregorian | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | Dashboard holiday and birthday cards show only Gregorian date. For Hebrew-language users, add the Hebrew date (e.g. "כ"ב ניסן תשפ"ו") below the Gregorian date. Use `@hebcal/core` HDate (already a project dependency). Only visible when `language === 'he'`. Holiday cards: show holiday Hebrew date. Birthday cards: show Hebrew birthday if contact has `hebrewBirthday` set. | Hebrew date visible on event cards in Hebrew mode; Gregorian-only in English mode; HDate from @hebcal/core; no regression; build+lint pass |
| BL-110 | BUG: Dashboard birthday countdown off-by-one for contacts with hebrewBirthday set | 🔴 High | 📋 Backlog | Sprint 31 | Bug | Agent 1 | When a contact has `hebrewBirthday` set, the dashboard countdown may be off by one day due to the Hebrew-to-Gregorian conversion not accounting for the current year's calendar offset. Investigate `hebrewBirthdayToGregorianInCalendarYear()` in `hebrewDateUtils.ts`; verify it returns the correct Gregorian date for this year; compare with `differenceInCalendarDays` (already used for DST safety). Fix any off-by-one. | Dashboard countdown correct for contacts with hebrewBirthday; same-day shows 0 (today); no regression on Gregorian birthday countdown; build+lint pass |
| BL-111 | Improvement: Calendar header — show Hebrew month name alongside Gregorian | 🟢 Low | 📋 Backlog | — | Improvement | Agent 1 | CalendarScreen header shows only the Gregorian month (e.g. "May 2026"). When `language === 'he'`, add the Hebrew month and year below (e.g. "אייר–סיוון תשפ"ו") using `@hebcal/core`. Display-only change — no calendar logic change. | Hebrew month name shown in calendar header in Hebrew mode; Gregorian-only in English mode; correct month pair for dates that span two Hebrew months; build+lint pass |
| BL-112 | BUG: Dashboard countdown resets incorrectly when app stays open past midnight | 🔴 High | ✅ Done | Sprint 31 | Bug | Agent 1 | `dashboardData` is computed via `useMemo` in `AppContext.tsx`. If the app stays open past midnight, the memoized countdowns become stale (e.g. an event that was "tomorrow" still shows "tomorrow" until the page is refreshed). Fix: add a date-change listener (check current date on `visibilitychange` event or a 1-minute interval); invalidate and recompute `dashboardData` when the calendar date changes. No change to countdown calculation logic. | Dashboard countdowns update automatically when calendar date changes; stale counts not shown after midnight; no performance regression; build+lint pass |
| BL-113 | Improvement: Dashboard event cards — sort today first, then tomorrow, then ascending | 🟡 Medium | ✅ Done | Sprint 31 | Improvement | Agent 1 | Dashboard upcoming events may appear out of order (see also BL-085 for upcoming events list). Ensure dashboard alert cards are sorted: (1) today (daysUntil === 0) → (2) tomorrow (daysUntil === 1) → (3) ascending by daysUntil. Same-day events: stable sort by event name. Sorting only — no change to event calculation or UI. | Cards always in ascending proximity order; today before tomorrow; same-day stable; no regression; build+lint pass |
| BL-114 | Improvement: Dashboard "no upcoming events" empty state with value-driven messaging | 🟢 Low | 📋 Backlog | — | Improvement | Agent 1 | When no events are coming up in the next 30 days, dashboard shows a generic or blank state. Replace with: warm Hebrew message ("הכל שקט עכשיו — מצוין זמן להוסיף אנשי קשר חדשים!") + CTA to add contact (triggers QuickAddModal). EN equivalent. Does not affect the existing empty-state for zero contacts. | "No upcoming events" message shown with Hebrew text; CTA visible; QuickAddModal triggers on tap; RTL correct; build+lint pass |
| BL-115 | Feature: Hebrew Privacy Policy screen accessible from Settings → About | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | No in-app privacy policy exists. Add a `/privacy` route with a Hebrew-language privacy policy screen covering: what data is stored (localStorage only for anonymous, Supabase for authenticated), what is NOT shared, contact deletion rights. Accessible via Settings → About section. Simple scrollable text screen — no external link. English version optional for MVP. Aligns with trust-building goals and "האפליקציה לא שולחת הודעות אוטומטית" philosophy. | Privacy policy screen exists at `/privacy`; linked from Settings; Hebrew text covers data storage, sharing policy, and user rights; RTL correct; build+lint pass |
| BL-116 | Feature: Hebrew Terms of Use screen accessible from Settings → About | 🟡 Medium | 📋 Backlog | — | Feature | Agent 1 | No terms of use screen exists. Add a `/terms` route with a brief Hebrew terms of use covering: app purpose, user responsibility for sending greetings, disclaimer that NeverMiss does not auto-send messages, support contact placeholder. Accessible via Settings → About. Simple scrollable text screen. | Terms screen at `/terms`; linked from Settings → About; Hebrew text; key disclaimer "האפליקציה לא שולחת הודעות אוטומטית" prominently included; RTL; build+lint pass |
| BL-117 | Improvement: Greeting send screen — "האפליקציה לא שולחת הודעות אוטומטית" disclaimer visible before channel selection | 🔴 High | 📋 Backlog | — | Improvement | Agent 1 | CLAUDE.md mandates this disclaimer on the sending screen. Audit `GreetingEditorScreen` and `ChannelPicker` — if the disclaimer is absent or not prominent, add it as a small note above or below the channel picker: "יש לשלוח ידנית — האפליקציה לא שולחת הודעות אוטומטית" (EN: "Send manually — this app does not send messages automatically"). One line of text. Always visible, never behind a toggle. EN+HE. | Disclaimer visible before channel selection; Hebrew text correct; always visible (not hidden); build+lint pass |
| BL-118 | Improvement: Anonymous user data persistence warning on first use | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | Anonymous users store all data in localStorage with no cloud backup. On first contact save (or first app open after onboarding for anonymous sessions), show a one-time dismissible banner: "הנתונים שלך נשמרים רק במכשיר זה. כדי לגבות — צור חשבון בחינם." + "צור חשבון" CTA + "המשך בלי חשבון" dismiss. Show once per session via localStorage flag. Never shown to authenticated users. | Banner shown once for anonymous users; Hebrew text; account CTA navigates to login; dismiss sets flag; never shown to authenticated users; build+lint pass |
| BL-119 | Feature: Anonymous → authenticated data migration — carry localStorage data into Supabase on first login | 🔴 High | 📋 Backlog | — | Feature | Agent 1 | When an anonymous user logs in or registers for the first time, their existing localStorage contacts, groups, and preferences should be migrated to their Supabase account. Flow: (1) Detect that localStorage has data AND user just authenticated for the first time; (2) Ask: "נמצאו נתונים קיימים — האם לשמור אותם בחשבון?" with Yes/No; (3) On Yes: write localStorage data to Supabase via dataService; (4) On No: keep Supabase empty, localStorage cleared. BLOCKED by BL-031 (Supabase CRUD layer). Planning/design only until BL-031 complete. | Migration prompt shown on first login if localStorage has data; Yes migrates; No clears local; no data loss on affirmative; build+lint pass |
| BL-120 | Improvement: Login/register screens — "Continue without account" CTA prominent | 🔴 High | ✅ Done | Sprint 31 | Improvement | Agent 1 | Anonymous-first MVP philosophy requires that anonymous use is the path of least resistance. The auth screens introduced in Sprint 30 must have a clearly visible "המשך בלי חשבון" / "Continue without account" CTA — not hidden below the fold or styled as a secondary text link. CTA should be visually equal to or more prominent than the login CTA. No behavior change — this CTA already routes to the app; adjust visibility and styling only. | "Continue without account" CTA visible above-the-fold on login screen; visually prominent; routes correctly to app; no regression in login flow; build+lint pass |
| BL-121 | Improvement: Post-auth redirect — return user to the screen they were on before the auth gate | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | When AuthGate redirects to login (session expired or direct link), after successful authentication the user lands on the default dashboard rather than the screen they originally intended to reach. Fix: capture the attempted route before redirect (store in sessionStorage as `auth_redirect_to`); after successful auth, redirect to captured route. Standard auth redirect pattern. | User returns to intended screen after login; route captured before redirect; no infinite redirect loops; build+lint pass |
| BL-122 | Improvement: Auth error messages fully in Hebrew with actionable next steps | 🔴 High | ✅ Done | Sprint 31 | Improvement | Agent 1 | Supabase auth errors (invalid email, rate limit exceeded, magic link expired, network failure) may surface in English or as raw Supabase error codes. Map all known Supabase auth error codes to Hebrew user-facing messages with actionable next steps (e.g. "קישור ההתחברות פג — יש לבקש קישור חדש"). Add error mapping utility to `src/context/AuthContext.tsx` or a new `src/utils/authErrors.ts`. EN+HE. | All known Supabase auth errors shown in Hebrew; actionable next steps included; no raw error codes or English visible to Hebrew user; build+lint pass |
| BL-123 | Improvement: Login screen — NeverMiss branding + one-line value prop above email input | 🟢 Low | 📋 Backlog | — | Improvement | Agent 1 | Login screen introduced in Sprint 30 likely lacks NeverMiss branding identity. Add: app name/logo at top + one-line value proposition ("לא תפספס יותר אף רגע חשוב") above the email input. Visual only — no behavior change. Consistent with the landing page emotional positioning. | App name/logo visible on login screen; Hebrew tagline above email input; RTL correct; no behavior change; build+lint pass |
| BL-124 | Feature: Global ErrorBoundary in App.tsx — prevents white screen on unhandled React errors | 🔴 High | ✅ Done | Sprint 31 | Feature | Agent 1 | Phase 1 requirement (CLAUDE.md) that has not been implemented. Add a top-level `<ErrorBoundary>` component wrapping the router in `App.tsx`. On uncaught React error: show a friendly Hebrew error screen ("משהו השתבש — יש לרענן את הדף") with a Refresh button. Log error details to console. Component: `src/components/ErrorBoundary.tsx` (new file). Class component required for React error boundaries. EN+HE error screen. | ErrorBoundary wraps app in App.tsx; unhandled errors show Hebrew error screen with Refresh CTA; no white screen; error logged to console; build+lint pass |
| BL-125 | BUG: AuthContext race condition — session check and AppContext data load may interleave on cold start | 🔴 High | 📋 Backlog | Sprint 31 | Bug | Agent 1 | Sprint 30 introduced async Supabase session check in `AuthContext`. `AppContext` initializes localStorage data synchronously. On cold start, there is a potential race: AppContext may attempt to resolve user-scoped data before auth state is known, or render decisions may be made before `isLoading` settles. Audit the initialization sequence in `App.tsx`, `AuthContext.tsx`, and `AppContext.tsx`; verify load order is deterministic; add a guard if needed. | Auth state resolved before AppContext makes auth-dependent decisions; initialization order is deterministic; no race-condition symptoms (flicker, wrong data, stale user); build+lint pass |
| BL-126 | Improvement: Supabase auth error capture — log specific failure reasons in dev/debug mode | 🟢 Low | 📋 Backlog | — | Improvement | Agent 1 | Auth failures in `AuthContext.tsx` are likely caught generically. In development mode (`import.meta.env.DEV`), log the full Supabase error object to console so debugging is faster. In production: log only the error code (no user data). No UI change. | Auth errors logged with detail in dev mode; error code only in prod; no user data leaked; build+lint pass |
| BL-127 | Improvement: Import/export operations respect authenticated vs anonymous context | 🟡 Medium | 📋 Backlog | — | Improvement | Agent 1 | CSV export/import currently reads/writes localStorage directly. When a user is authenticated and Supabase data layer (BL-031) is active, import/export should read from and write to the correct data source. Design the import/export service to accept a `dataContext` parameter (localStorage vs Supabase) so the routing is clean when BL-031 lands. No behavior change for anonymous users. Planning/interface design only until BL-031 is complete. | Import/export service accepts dataContext; anonymous path unchanged; Supabase path ready for BL-031; no regression; build+lint pass |
| BL-129 | Feature: Cloud backup (Premium) — export full app data snapshot to Supabase user storage | 🟢 Low | 📋 Backlog | — | Feature | Agent 1 | Premium feature: allow authenticated users to take a manual snapshot of all their app data (contacts, groups, settings) and store it in Supabase (user_data table or storage bucket). One snapshot at a time — new snapshot overwrites previous. Restore flow: load snapshot back into app. Companion to BL-034 (JSON backup). BLOCKED by BL-031 (Supabase CRUD). Premium gate. | Backup CTA in Settings (Premium); snapshot saved to Supabase; restore loads data back; overwrites existing data with confirmation; build+lint pass |
| BL-130 | BUG: Settings Account section shows stale user email after session refresh or re-login | 🟡 Medium | 📋 Backlog | — | Bug | Agent 1 | The Account section in SettingsScreen (introduced Sprint 30) reads `user.email` from AuthContext. If the session is refreshed or user re-authenticates with a different email, the displayed email may remain stale until a full page reload. Fix: ensure `SettingsScreen` subscribes to AuthContext user object reactively (not a snapshot) so the displayed email updates when auth state changes. | Settings Account section shows current email in real time; updates on session refresh or re-login; no page reload required; build+lint pass |
| BL-131 | UX: Persistent account access entry point in top navigation | 🟡 Medium | 📋 Backlog | — | UX / Navigation | Agent 1 | Account access and auth identity are currently only visible inside SettingsScreen — requiring 2+ taps to discover. Authenticated identity is not globally visible and anonymous users have no persistent CTA to create an account outside the landing screen. Add a persistent account entry point to the top navigation or page header area, visible across all primary screens (Dashboard, Contacts, Calendar, Groups). Authenticated users: avatar or account icon showing active session. Anonymous users: "התחבר / צור חשבון" CTA. Tapping opens the existing auth flow (no new auth logic). No backend changes. No new routes. RTL-aware. EN+HE. | Account entry point visible on primary screens without entering Settings; anonymous CTA accessible from main app; authenticated state globally visible; RTL correct; mobile-safe; no navigation regressions; build+lint pass |
| BL-133 | UX: Mobile browser bottom-bar overlap on landing screen | 🟡 Medium | ✅ Done | Sprint 31 stabilization | UX / Mobile | Agent 1 | Android Chrome and Samsung Internet bottom navigation bars (~56px) visually overlap the lower CTA area of LandingScreen. `env(safe-area-inset-bottom)` is 0 on Android so the previous safe-area fix did not help. The bottom spacer minimum was too low (2.5rem/40px) to clear the browser toolbar. Fix: raise spacer minimum to 5rem (80px) — clears Android browser chrome with breathing room; safe-area-inset-bottom term continues to handle iOS home indicator. | Bottom CTA area not clipped on Android Chrome or Samsung Internet; visible breathing room above browser bar; no excessive whitespace on desktop; no regression on iOS or desktop; build+lint pass |

---

## Archive

> Tasks completed 2+ sprints before current sprint. Never deleted — reference only.

| Task ID | Task | Sprint Completed | Completion Notes |
|---------|------|-----------------|-----------------|
| ARC-001 | storageService.ts silent catch | Iter. 1 | Added console.warn on JSON.parse failure (BUG-001) |
| ARC-002 | AppContext whole-tree re-render fix | Iter. 2 | Wrapped provider value in useMemo |
| ARC-003 | avatarUtils.ts — gradient consistency | Iter. 3 | Extracted to shared util with Map cache; fixed 8→10 gradient count (BUG-002) |
| ARC-004 | AppContext useMemo dashboardData | Iter. 4 | Replaced useState+useCallback+useEffect triple with useMemo |
| ARC-005 | HolidayCard full i18n | Iter. 5 | today/tomorrow/calendar_passed/days keys via t() |
| ARC-006 | GreetingEditorScreen i18n + labelHe removal | Iter. 6 | Removed labelHe hack; 8 strings via t(); 11 new i18n keys |
| ARC-007 | BUG-003–019: 17 lint errors | Iter. 5 | Agent 5 fixed across 9 files |
| ARC-008 | Feature Sprint F1: Greeting Media | Sprint F1 | MediaAttachmentPicker + GreetingEditor + WhatsAppButton image clipboard |
| ARC-009 | Feature Sprint F1: Multi-Channel Sending | Sprint F1 | Channel picker (WhatsApp/SMS/Email/Copy/Share) |
| ARC-010 | Feature Sprint F1: Group Holiday Assignment | Sprint F1 | holidayIds on Group; dashboard alerts |
| ARC-011 | Feature Sprint F1: Coupon System | Sprint F1 | NEVERMISS1/WELCOME2025/ISRAEL30; expiry logic; Settings display |
| ARC-012 | CalendarScreen key={h.id} fix | Iter. 7 | BUG-022 fixed — holiday dot reconciliation |
| ARC-013 | ContactCard div→button (WCAG 2.1.1) | Iter. 8 | Keyboard-accessible contact cards |
| ARC-014 | .card-interactive:focus-visible ring | Iter. 9 | WCAG 2.4.7 focus visible on cards |
| ARC-015 | WhatsApp phone normalization | Iter. 10 | Israeli local numbers (052-xxx → E.164) |
| ARC-016 | BUG-031–035 | Iter. 11 | CSS calc, 00-prefix, empty guard, execCommand, useMemo deps |
| ARC-017 | Hebrew calendar integration | Iter. 11 | @hebcal/core HDate + gematriya in CalendarScreen |
| ARC-018 | QA infrastructure | Iter. 11 | qa_status.json, make-qa-checklist.js, NeverMiss_QA_Checklist.xlsx, changelog_queue.json |
| ARC-019 | Modal focus trap | Sprint F1 | Tab/Shift+Tab trapped; focus restored on close |
| ARC-020 | Premium system | Sprint F1 | Free (20 contacts) / Premium gating; coupon redemption; expiry |
| ARC-021 | BUG-023 isOnboardingDone every render | Iter. 4 | Moved to useMemo in App.tsx (line 76) |
